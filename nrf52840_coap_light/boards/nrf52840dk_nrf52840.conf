# nRF52840 DK 보드 전용 추가 설정

# USB CDC 시리얼 (디버그용 – 배포 시 제거하면 ~0.5 mA 절약)
CONFIG_USB_DEVICE_STACK=y
CONFIG_USB_CDC_ACM=y
CONFIG_UART_LINE_CTRL=y

# 온칩 플래시 절전
CONFIG_SOC_FLASH_NRF_UICR_APPROTECT_DISABLE=y
